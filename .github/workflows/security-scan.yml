name: Security Scan

on:
  pull_request:
    paths:
      - ".github/workflows/security-*.yml"

jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security scan executed"
